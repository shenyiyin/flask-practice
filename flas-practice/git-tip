前提git pull origin master --allow-unrelated-histories 更新一下本地git仓库
1.cd到当前文件夹
2.git init
3.git add .
4.本地库上传到远程git仓库git push -u origin master   ----1、先输入 git remote rm origin
                                  2、再输入 git remote add origin**************(git网址https://github.com/shenyiyin/flask-practice)
                                  3.git push -u origin master
                                  一般就能直接上传了

5.弹出access taken输入仓库密钥


清空远程库
1.git add *        将本地文件上传缓存
2.git commit -m 'del'  发送删除指令
3.重新上传我们要的
